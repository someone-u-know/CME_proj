package org.example.testcases;
import java.util.*;
import org.example.entity.User;

public class SampleTestCases {

    // Test searchUserByName
//    public void testSearchUserbyName(NetworkMethods network, String name) {
//        System.out.println("Searching for users with name: " + name);
//
//        List<User> names = network.searchUserByName(name);
//
//        // Check if names list is null or empty
//        if (names == null || names.isEmpty()) {
//            System.out.println("No users found with the name: " + name);
//            System.out.println("searchUserByName test failed.");
//            return;
//        }
//
//        // Print each user found
//        for (User user : names) {
//            System.out.println(user);
//        }
//
//        // Check if all users in the list have the name matching 'name'
//        boolean allMatch = names.stream().allMatch(user -> user.getName().equals(name));
//
//        // Verify conditions
//        if (names.size() == 2 && allMatch) {
//            System.out.println("searchUserByName test passed.");
//        } else {
//            System.out.println("searchUserByName test failed.");
//        }
//    }
}

